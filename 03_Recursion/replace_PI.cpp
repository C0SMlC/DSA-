#include <bits/stdc++.h>
using namespace std;
void check(string str)
{
    if (str.length() == 0 or str.length() == 1)
    {
        return;
    }
}
int main()
{
    string s;
    cin >> s;
    cout << s;
    // cout << s.length();
    return 0;
}